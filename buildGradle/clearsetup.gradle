tasks.register("clearSetup") {
    group = "Translate Core"
    description = "Removes Translate Core"
    doLast {
        file("CoreToFolia").deleteDir()
        file("TranslateCore").deleteDir()
    }
}